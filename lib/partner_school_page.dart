import 'package:flutter/material.dart';

class PartnerSchoolPage extends StatelessWidget {
  const PartnerSchoolPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Partner as a School')),
      body: const Center(
        child: Text(
          'Partner as a School page coming soon!',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
