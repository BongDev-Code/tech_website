import 'package:flutter/material.dart';

class JoinSchoolPage extends StatelessWidget {
  const JoinSchoolPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Join as a School')),
      body: const Center(
        child: Text(
          'Join as a School page coming soon!',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}