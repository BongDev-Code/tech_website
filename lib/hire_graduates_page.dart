import 'package:flutter/material.dart';

class HireGraduatesPage extends StatelessWidget {
  const HireGraduatesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hire Our Graduates')),
      body: const Center(
        child: Text(
          'Hire Our Graduates page coming soon!',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}