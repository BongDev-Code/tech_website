import 'package:flutter/material.dart';

class VolunteerPartnerPage extends StatelessWidget {
  const VolunteerPartnerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Volunteer or Partner')),
      body: const Center(
        child: Text(
          'Volunteer or Partner page coming soon!',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}